# Notion-as-CMS Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: Notion Integration
# =============================================================================

# Your Notion integration token (starts with "secret_")
# Get this from: https://www.notion.so/my-integrations
NOTION_TOKEN=secret_your_notion_integration_token_here

# Database ID for form submissions (contact form, leads, etc.)
# Find this in the database URL: notion.so/workspace/[DATABASE_ID]?v=...
NOTION_DATABASE_ID=your_32_character_database_id_here

# =============================================================================
# PAGE TYPE CONFIGURATION (Optional)
# =============================================================================
# Set parent page IDs to enable automatic page type detection.
# Child pages inherit the layout style of their configured parent.
# Pages not under any parent default to "landing" style.

# Blog parent page ID - children render with article layout (dates, share buttons)
NOTION_BLOG_PAGE_ID=your_32_character_blog_parent_id

# Landing pages parent ID - children render with full-width marketing layout
NOTION_LANDING_PAGE_ID=your_32_character_landing_parent_id

# Documentation parent ID - children render with sidebar + table of contents
NOTION_DOCS_PAGE_ID=your_32_character_docs_parent_id

# Homepage page ID - the Notion page to use as your site homepage
# If not set, a static fallback homepage is displayed
NOTION_HOMEPAGE_ID=your_32_character_homepage_id

# =============================================================================
# CONTENT WORKFLOW
# =============================================================================

# Secret key for previewing draft content (used in ?preview=YOUR_SECRET)
# Generate a random string: openssl rand -hex 16
PREVIEW_SECRET=your_preview_secret_here

# =============================================================================
# SECURITY & DEPLOYMENT
# =============================================================================

# Your deployed site URL (for CORS configuration)
# Example: https://mysite.netlify.app or https://www.mydomain.com
SITE_URL=https://your-site-url.netlify.app

# Environment: "development" for local, "production" for deployed
NODE_ENV=development

# =============================================================================
# OPTIONAL NOTION PAGE PROPERTIES
# =============================================================================
# Add these properties to your Notion pages for enhanced functionality.
# All properties are optional - sensible defaults are used if not set.
#
# CONTENT WORKFLOW:
# - Status (select): Draft | Published | Scheduled
#   Controls visibility. Drafts hidden unless ?preview=SECRET
#
# - Publish Date (date): Scheduled publish date
#   Used with Status=Scheduled for timed publishing
#
# SEO & URLS:
# - Slug (text): Custom URL slug override
#   e.g., "my-awesome-post" instead of auto-generated
#
# - Meta Title (text): SEO title (overrides page title)
#
# - Meta Description (text): SEO description for search results
#
# BLOG FEATURES:
# - Pinned (checkbox): Pin post to top of list
#
# - Featured (checkbox): Mark as featured content
#
# - Sort Order (number): Custom sort priority (higher = first)
#
# DOCS FEATURES:
# - Nav Title (text): Shorter title for sidebar navigation
#
# - Sort Order (number): Custom order in sidebar (higher = first)
